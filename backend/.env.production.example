# Production Environment Variables for aescion-backend

# --- Server Configuration ---
PORT=3000
# Important: This sets the base URL for images and API calls.
# Since we using Nginx to proxy /api -> backend, this must include /api
API_URL=https://aesciontech.com/api

# --- Database (SQLite) ---
# When using SQLite, the host/user/pass fields are ignored but kept for reference.
DB_TYPE=sqlite
# DB_HOST=localhost
# DB_PORT=3306
# DB_USERNAME=aescion_user
# DB_PASSWORD=SecurePassword123
# DB_DATABASE=aescion_db

# --- Security ---
JWT_SECRET=supersecretkey123
# CORS Origin should match your frontend domain
CORS_ORIGIN=https://aesciontech.com,https://www.aesciontech.com

# --- AWS S3 (Gallery) ---
AWS_S3_BUCKET=aescion-gallery
AWS_REGION=us-east-1
# YOU MUST REPLACE THESE WITH REAL KEYS for S3 to work!
# Do not use 'mock_key' in production if you want images to be stored in S3.
AWS_ACCESS_KEY_ID=YOUR_REAL_AWS_ACCESS_KEY_ID
AWS_SECRET_ACCESS_KEY=YOUR_REAL_AWS_SECRET_KEY

# --- Email ---
MAIL_USER=contact.aescion@gmail.com
MAIL_PASS=jbhsueurzridtxcw
